# Shared Native Utilities

Common native code utilities (string conversion, memory helpers, logging, threading adapters) shared by all platform implementations will live here. Code will be written in portable C99/C++17 as appropriate to minimize duplication.

